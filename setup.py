from distutils.core import setup

setup(
  name = 'uoftscrapers',
  packages = ['uoftscrapers'], # this must be the same as the name above
  version = '0.1',
  description = 'University of Toronto public web scraping scripts.',
  author = 'Qasim Iqbal',
  author_email = 'me@qas.im',
  url = 'https://github.com/cobalt-io/uoft-scrapers', # use the URL to the github repo
  download_url = 'https://github.com/cobalt-io/uoft-scrapers/tarball/0.1', # I'll explain this in a second
  keywords = ['uoft', 'scraper', 'toronto'], # arbitrary keywords
  classifiers = [],
)
